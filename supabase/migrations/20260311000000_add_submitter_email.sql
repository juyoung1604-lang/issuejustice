-- ============================================================
-- Migration 005: 제보자 이메일 컬럼 추가
-- ============================================================
-- 이슈 제보 시 이메일 주소를 함께 저장 (OTP 인증 없이 단순 보관)
-- 관리자가 검토 결과 알림 등에 활용 가능

ALTER TABLE issues ADD COLUMN IF NOT EXISTS submitter_email TEXT;
